<h1>My Bookings</h1>

<ul>
  <% @bookings.each do |booking| %>
    <li>
      <%= booking.pet.name %> |
      <%= booking.pet.species.name.capitalize %> |
      From: <%= booking.start_date %>
      To: <%= booking.end_date %>
    </li>
    <%= link_to 'Delete', booking_path(booking), method: :delete %>
  <% end %>
  <br>
  <%= link_to 'Back', pets_path %>
</ul>
